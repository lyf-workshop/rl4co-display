[pytest]
# Pytest配置文件

python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 测试路径
testpaths = tests

# 输出选项
addopts = 
    -v
    --tb=short
    --strict-markers

# 标记定义
markers =
    unit: 单元测试
    integration: 集成测试
    slow: 慢速测试


